---
title: "Image Fleet by Category: Native FreeBSD OCI Images"
description: "Browse the full collection of Daemonless OCI images for FreeBSD, grouped by category."
hide:
  - toc
---

<!-- This file is auto-generated by scripts/generate_docs.py. Do not edit directly — edit scripts/templates/status-categories.mkdocs.j2 instead. -->

# Image Fleet by Category

Browse our FreeBSD-native OCI images grouped by category. All images are compliant with OCI standards and ready for any compatible runtime.

[:material-format-list-bulleted: View alphabetical list](index.md){ .md-button }

<div class="image-search-container">
  <input type="text" id="image-search" placeholder="Search Image Fleet..." aria-label="Search Image Fleet">
</div>

{% set active_configs = configs | rejectattr('deprecated') | list %}

{# Categories are derived from each image's `category` field — fully dynamic.
   Re-categorize in the compose files and re-run generate; sections follow. #}
{% for category, items in active_configs | sort(attribute='title') | groupby('category') %}
## {{ category }}

| Image | Description | Links | Last Update |
|-------|-------------|-------|:------------:|
{%- for c in items %}
| [{{ c.icon }} **{{ c.title }}**]({{ c.name }}.md) | {{ c.description }} | [:simple-github:]({{ c.repo_url }}){target=_blank} {% if c.upstream_url %} · [:material-link-variant:]({{ c.upstream_url }}){target=_blank}{% endif %} | [{{ c.last_update }}]({{ c.repo_url }}/commits){target=_blank} |
{%- endfor %}

{% endfor %}

<style>
.image-search-container {
  margin: 2rem 0;
}

#image-search {
  width: 100%;
  padding: 0.6rem 1rem;
  border-radius: 4px;
  border: 1px solid var(--md-default-fg-color--lightest);
  background: var(--md-code-bg-color);
  color: var(--md-default-fg-color);
  font-family: inherit;
  font-size: 0.9rem;
  box-shadow: 0 2px 5px rgba(0,0,0,0.1);
  transition: border-color 0.2s, box-shadow 0.2s;
}

#image-search:focus {
  outline: none;
  border-color: var(--md-primary-fg-color);
  box-shadow: 0 0 0 1px var(--md-primary-fg-color);
}

.md-typeset table {
  width: 100%;
}

h2 {
  border-bottom: 2px solid var(--md-primary-fg-color--light);
  padding-bottom: 0.5rem;
  margin-top: 3rem;
}
</style>
